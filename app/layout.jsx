import "./globals.css";

export const metadata = {
  title: "FAULTLINE",
  description: "Physics reasoning checker.",
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
