import Link from "next/link";
import { PACKS } from "@/content/packs";
export default function Home() { return <main className="shell"><header className="mast"><span className="mark">FAULTLINE</span><Link className="mono" href="/evaluation">EVALUATION</Link></header><section className="intro"><p className="kicker">Physics · causal evidence · local simulation</p><h1>Find the relationship<br/>that breaks the model.</h1><p>Choose a bounded scenario. Faultline separates the answer from the method, then tests a targeted counterexample.</p></section><section className="scenario-list" aria-label="Physics scenarios">{PACKS.map((p,i)=><Link className="scenario" href={`/learn/${p.id}`} key={p.id}><span className="scenario-num">0{i+1}</span><div><h2>{p.title}</h2><p>{p.misconception}</p></div><span aria-hidden="true">→</span></Link>)}</section><footer>Designed to help reveal and repair misconceptions. Not a chatbot.</footer></main> }
