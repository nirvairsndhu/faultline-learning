import { test,type Page } from "@playwright/test";
import { mkdirSync } from "node:fs";
mkdirSync("public/screenshots",{recursive:true});
async function toLab(page:Page,id:"vacuum-drop"|"collision-forces"|"projectile-motion",answer:string,reason:string){await page.goto(`/learn/${id}`);await page.getByLabel(answer).check();await page.getByRole("button",{name:"Explain your method"}).click();await page.getByLabel("Reasoning").fill(reason);await page.getByRole("button",{name:"Map reasoning"}).click();}
test("capture production visual evidence",async({page},testInfo)=>{const shot=(name:string)=>page.screenshot({path:`public/screenshots/${name}.png`,fullPage:true});if(testInfo.project.name==="mobile"){await page.goto("/");await shot("mobile-scenario");await toLab(page,"vacuum-drop","They land together","Mass causes acceleration.");await shot("mobile-graph");await page.getByRole("button",{name:"Make prediction"}).click();await page.getByLabel("They land together").check();await page.getByRole("button",{name:"Unlock simulation"}).click();await shot("mobile-simulation");return;}await page.setViewportSize({width:1280,height:720});await page.goto("/");await shot("scenario-selection");await page.getByRole("link",{name:"Vacuum Drop"}).click();await shot("reliability-mode");await page.getByLabel("They land together").check();await page.getByRole("button",{name:"Explain your method"}).click();await shot("explanation-input");await page.getByLabel("Reasoning").fill("Mass causes acceleration.");await page.route("**/api/analyze",async route=>{await new Promise(resolve=>setTimeout(resolve,300));await route.continue();},{times:1});await page.getByRole("button",{name:"Map reasoning"}).click();await page.getByRole("status").waitFor();await shot("analysis-loading");await page.getByText("A relationship is unsupported.").waitFor();await shot("hidden-misconception");await page.getByRole("button",{name:"Make prediction"}).click();await shot("vacuum-prediction");await page.getByLabel("They land together").check();await page.getByRole("button",{name:"Unlock simulation"}).click();await page.getByRole("button",{name:"Run simulation"}).click();await shot("vacuum-observation");await page.getByRole("button",{name:"Explain again"}).click();await shot("teach-back");await page.getByLabel("Revised reasoning").fill("Mass still matters.");await page.getByRole("button",{name:"Verify repair"}).click();await shot("failed-repair");await page.getByRole("button",{name:"Revise explanation"}).click();await page.getByLabel("Revised reasoning").fill("In vacuum both objects have the same acceleration independent of mass.");await page.getByRole("button",{name:"Verify repair"}).click();await shot("successful-graph-repair");await page.getByRole("button",{name:"Test transfer"}).click();await page.getByLabel("The 8 kg tool accelerates more").check();await page.getByLabel("Why?").fill("Mass causes more acceleration.");await page.getByRole("button",{name:"Verify transfer"}).click();await shot("transfer-failure");await page.getByLabel("They have the same acceleration and land together").check();await page.getByLabel("Why?").fill("Both have the same acceleration in a vacuum.");await page.getByRole("button",{name:"Verify transfer"}).click();await shot("transfer-success");await shot("session-report");await toLab(page,"collision-forces","Equal magnitude, opposite directions","Truck has more mass so it exerts more force.");await page.getByRole("button",{name:"Make prediction"}).click();await page.getByLabel("Equal magnitude, opposite directions").check();await page.getByRole("button",{name:"Unlock simulation"}).click();await shot("collision-simulation");await toLab(page,"projectile-motion","No horizontal force is required","A forward force continues after release.");await page.getByRole("button",{name:"Make prediction"}).click();await page.getByLabel("No horizontal force is required").check();await page.getByRole("button",{name:"Unlock simulation"}).click();await shot("projectile-simulation");await toLab(page,"vacuum-drop","They land together","Maybe they land together, but I am not sure.");await page.getByText("Clarification needed.").waitFor();await shot("uncertain-diagnosis");await shot("diagnostic-follow-up");await page.emulateMedia({reducedMotion:"reduce"});await page.goto("/learn/vacuum-drop");await shot("reduced-motion");});
