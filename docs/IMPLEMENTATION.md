# Implementation

The UI renders authored packs. Deterministic domain functions own physical calculations, graph checks, transfer rules, and final completion. The bounded analysis endpoint returns a disclosed fixture when live inference is unavailable. No secret is exposed to the browser.
