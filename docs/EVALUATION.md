# Evaluation

`npm run evaluate` regenerates machine-readable and Markdown fixture-mode results. The included labels are authored reference labels; independent human review is pending. No live-model metrics are claimed without a configured key and real run.
