# Evaluation

`npm run evaluate` regenerates machine-readable and Markdown fixture-mode results from exactly 30 authored cases: 10 per pack, with three valid methods, three clear misconceptions, two hidden misconceptions, and two ambiguous cases per pack. The included labels are authored reference labels; independent human review is pending. No live-model metrics are claimed without a configured key and real run.
